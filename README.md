# restaurants-ms

1. clonar repositorio y desde el directorio raiz ejecutar './mvnw clean install' para compilar y ejecutar los test unitarios.

2. ejecutar con 'mvnw clean spring-boot:run' esto levantará el servicio tirando contra la bbdd creada en Azure.  (debería estar disponible)

